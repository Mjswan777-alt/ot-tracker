<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Roster display showing each worker's name, opt-out status, and OT count -->
    <TextView
        android:id="@+id/rosterView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:text="Roster will show here"
        android:textAppearance="?android:textAppearanceMedium" />

    <!-- Summary display for showing the result of OT assignment or status updates -->
    <TextView
        android:id="@+id/summaryView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:text="Summary will show here"
        android:textAppearance="?android:textAppearanceMedium" />

    <!-- Horizontal button bar for assigning OT, toggling opt-out, logging work, and viewing history -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="8dp">

        <Button
            android:id="@+id/assignButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Assign OT" />

        <Button
            android:id="@+id/optOutButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Opt-Out" />

        <Button
            android:id="@+id/workedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Worked Today" />

        <Button
            android:id="@+id/historyButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="History" />
    </LinearLayout>

</LinearLayout>
